name: CI/CD Pipeline
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: [frontend, backend, mobile]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
      - run: npm install -g pnpm
      - run: cd ${{ matrix.service }} && pnpm install
      - run: cd ${{ matrix.service }} && pnpm run lint
      - run: cd ${{ matrix.service }} && pnpm run test
      - run: cd ${{ matrix.service }} && pnpm run build
  
  build-docker:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4
      - name: Build and push Docker images
        run: |
          docker build -t rivaya/web:${{ github.sha }} -f frontend/Dockerfile .
          docker build -t rivaya/api:${{ github.sha }} -f backend/Dockerfile .
